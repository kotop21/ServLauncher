[project]
name = "core"
version = "0.0.2"
description = "Core utils for Astra Launcher"
requires-python = ">=3.13"
dependencies = [
    "certifi>=2026.5.20",
]

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"
